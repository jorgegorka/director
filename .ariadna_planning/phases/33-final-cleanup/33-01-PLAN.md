---
phase: 33-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/hooks/validation_processor.rb
  - app/models/roles/gate_check.rb
  - test/jobs/execute_agent_job_test.rb
  - app/services/ (deleted directory)
autonomous: true
user_setup: []
domain: general
must_haves:
  truths:
    - "No file in the codebase (app/, test/, config/, lib/) references any of the 13 old service class names -- grep confirms zero hits for WakeRoleService, GateCheckService, EmergencyStopService, ExecuteHookService, ProcessValidationResultService, BudgetEnforcementService, GoalEvaluationService, HeartbeatScheduleManager, CreateDocumentService, ApplyRoleTemplateService, ApplyAllRoleTemplatesService, bare RoleTemplateRegistry, bare AiClient"
    - "The full test suite passes with zero failures and zero errors"
    - "The app/services/ directory no longer exists on disk"
    - "At least one code quality improvement is committed -- the N+1 fix in Hooks::ValidationProcessor, the Auditable concern usage in Roles::GateCheck, and the tmux stub safety in ExecuteRoleJobTest"
    - "Rubocop passes with zero offenses"
    - "Brakeman reports no new warnings beyond the pre-existing permit! in RoleHooksController"
  artifacts:
    - path: app/models/hooks/validation_processor.rb
      provides: "N+1 fix -- build_feedback_body uses .includes(:author) and caches @validation_messages for reuse in record_audit_event, eliminating extra COUNT query"
    - path: app/models/roles/gate_check.rb
      provides: "Uses role.record_audit_event! (Auditable concern) instead of direct AuditEvent.create!, consistent with all other domain objects"
    - path: test/jobs/execute_agent_job_test.rb
      provides: "Global tmux stub in setup/teardown prevents flaky real process spawning; specific adapter tests override as needed"
  key_links:
    - from: app/models/hooks/validation_processor.rb#build_feedback_body
      to: validation_task.messages.includes(:author)
      via: ActiveRecord includes (N+1 prevention)
    - from: app/models/roles/gate_check.rb#record_audit_event!
      to: Auditable#record_audit_event!
      via: concern method (consistent API)
---

<objective>
Finalize the v1.6 Service Refactor milestone by verifying all 13 old service references are gone, committing the three code quality improvements discovered during migration (N+1 fix, Auditable consistency, test stability), deleting the empty app/services/ directory, and confirming the full test suite and linters pass cleanly. This is the last phase -- after this, the milestone ships.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify zero stale references and commit code quality improvements</name>
  <files>
    app/models/hooks/validation_processor.rb
    app/models/roles/gate_check.rb
    test/jobs/execute_agent_job_test.rb
  </files>
  <reuses>none -- these are existing uncommitted changes from migration phases, not new code</reuses>
  <action>
    Step 1: Run grep across app/, test/, config/, and lib/ for each of the 13 old service class names. The full list:
      - WakeRoleService
      - GateCheckService
      - EmergencyStopService
      - ExecuteHookService
      - ProcessValidationResultService
      - BudgetEnforcementService
      - GoalEvaluationService
      - HeartbeatScheduleManager (bare, not Heartbeats::ScheduleManager)
      - AiClient (bare, not Agents::AiClient)
      - CreateDocumentService
      - RoleTemplateRegistry (bare, not RoleTemplates::Registry)
      - ApplyRoleTemplateService
      - ApplyAllRoleTemplatesService

    All 13 must return zero matches in code files (*.rb, *.erb, *.yml in app/, test/, config/, lib/). Planning docs (.ariadna_planning/) are excluded from this check.

    Step 2: Review and verify the three uncommitted code quality improvements already present in the working tree:

    (a) app/models/hooks/validation_processor.rb:
      - build_feedback_body now uses `.includes(:author)` on the messages query, fixing an N+1 noted in phase 20 verification
      - Results cached in @validation_messages instance variable
      - record_audit_event uses @validation_messages.size instead of a second COUNT query
      - Removed vestigial section comments ("--- Feedback message ---", etc.)

    (b) app/models/roles/gate_check.rb:
      - record_audit_event! now calls role.record_audit_event! (Auditable concern) instead of direct AuditEvent.create!
      - This makes GateCheck consistent with every other domain object in the codebase

    (c) test/jobs/execute_agent_job_test.rb:
      - Added global ClaudeLocalAdapter.spawn_session stub in setup block
      - Added matching teardown to remove the stub
      - Prevents real tmux process spawning during non-adapter-specific tests

    Step 3: Confirm all three changes are correct and complete. Do NOT modify these files further -- they are already in the correct state.
  </action>
  <verify>
    Run: grep -rn "WakeRoleService\|GateCheckService\|EmergencyStopService\|ExecuteHookService\|ProcessValidationResultService\|BudgetEnforcementService\|GoalEvaluationService\|CreateDocumentService\|ApplyRoleTemplateService\|ApplyAllRoleTemplatesService" app/ test/ config/ lib/ --include="*.rb" --include="*.erb" --include="*.yml"
    Expected: no output (zero matches)

    Run: grep -rn "\bAiClient\b" app/ test/ config/ lib/ --include="*.rb" | grep -v "Agents::AiClient"
    Expected: no output (all references are namespaced)

    Run: grep -rn "\bRoleTemplateRegistry\b" app/ test/ config/ lib/ --include="*.rb" | grep -v "RoleTemplates::Registry"
    Expected: no output

    Run: grep -rn "\bHeartbeatScheduleManager\b" app/ test/ config/ lib/ --include="*.rb" | grep -v "Heartbeats::ScheduleManager"
    Expected: no output
  </verify>
  <done>All 13 old service names return zero grep hits in code files. The three code quality improvements are verified correct.</done>
</task>

<task type="auto">
  <name>Task 2: Delete app/services/ directory</name>
  <files>app/services/ (directory removal)</files>
  <reuses>none -- directory deletion</reuses>
  <action>
    Step 1: Verify app/services/ is empty (contains zero files). Run: find app/services/ -type f
    Expected: no output.

    Step 2: Remove the directory: rm -rf app/services/

    Step 3: Verify it no longer exists: ls app/services/ should fail with "No such file or directory"

    Step 4: Verify Rails autoloader is not configured to look for it (it should not be, since Rails autoloads app/ subdirectories only when they contain .rb files, and the default config does not explicitly list app/services/). Check config/application.rb for any autoload_paths referencing services -- there should be none.
  </action>
  <verify>
    Run: test -d app/services && echo "FAIL: directory still exists" || echo "PASS: directory removed"
    Expected: "PASS: directory removed"

    Run: grep -n "services" config/application.rb
    Expected: no matches (no autoload path configured)
  </verify>
  <done>The app/services/ directory no longer exists on disk. No autoload configuration references it.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification suite and confirm clean state</name>
  <files>none (verification only)</files>
  <reuses>none</reuses>
  <action>
    Step 1: Run the full test suite: bin/rails test
    Expected: All tests pass with zero failures and zero errors. Current count is 1243 tests.

    Step 2: Run rubocop: bin/rubocop
    Expected: Zero offenses.

    Step 3: Run brakeman: bin/brakeman --quiet --no-pager
    Expected: 1 warning (the pre-existing permit! in RoleHooksController). No new warnings.

    Step 4: Run bundler-audit: bin/bundler-audit
    Expected: No vulnerabilities found.

    If any step fails, diagnose and fix the issue before proceeding. The milestone cannot ship with failing tests or new lint/security warnings.
  </action>
  <verify>
    bin/rails test exits with 0 failures, 0 errors
    bin/rubocop exits with 0 offenses
    bin/brakeman shows exactly 1 pre-existing warning (Mass Assignment in role_hooks_controller.rb:65)
  </verify>
  <done>Full CI suite passes: tests green, rubocop clean, brakeman shows only pre-existing warning, bundler-audit clean. The codebase is ready to ship v1.6.</done>
</task>
</tasks>

<success_criteria>
Phase 33 is complete when ALL of the following are true:
1. grep confirms zero hits for all 13 old service class names in app/, test/, config/, lib/
2. bin/rails test passes with zero failures and zero errors
3. app/services/ directory does not exist on disk
4. Three code quality improvements are committed: N+1 fix in ValidationProcessor, Auditable usage in GateCheck, tmux stub safety in ExecuteRoleJobTest
5. bin/rubocop reports zero offenses
6. bin/brakeman reports no new warnings
7. The v1.6 Service Refactor & Cleanup milestone is complete
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/33-final-cleanup/33-01-SUMMARY.md`
</output>
