---
phase: 29-roles-domain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/roles/waking.rb
  - test/models/roles/waking_test.rb
  - app/jobs/role_heartbeat_job.rb
  - app/models/concerns/triggerable.rb
  - app/controllers/task_questions_controller.rb
  - app/services/execute_hook_service.rb
  - app/services/goal_evaluation_service.rb
  - app/services/process_validation_result_service.rb
  - test/services/execute_hook_service_test.rb
autonomous: true
user_setup: []
domain: backend
must_haves:
  truths:
    - "Roles::Waking.call(role:, trigger_type:) creates a HeartbeatEvent and dispatches an ExecuteRoleJob -- identical behavior to the old WakeRoleService"
    - "RoleHeartbeatJob calls Roles::Waking instead of WakeRoleService"
    - "Triggerable concern calls Roles::Waking instead of WakeRoleService"
    - "TaskQuestionsController calls Roles::Waking instead of WakeRoleService"
    - "ExecuteHookService, GoalEvaluationService, and ProcessValidationResultService call Roles::Waking instead of WakeRoleService"
    - "All existing WakeRoleService tests pass under the new Roles::Waking namespace"
    - "No file outside .ariadna_planning/ references WakeRoleService"
  artifacts:
    - path: app/models/roles/waking.rb
      provides: "Roles::Waking -- relocated WakeRoleService with identical interface and behavior"
    - path: test/models/roles/waking_test.rb
      provides: "Roles::WakingTest -- relocated WakeRoleServiceTest with updated class references"
  key_links:
    - from: app/jobs/role_heartbeat_job.rb
      to: Roles::Waking.call
      via: direct method call
    - from: app/models/concerns/triggerable.rb
      to: Roles::Waking.call
      via: direct method call
    - from: app/controllers/task_questions_controller.rb
      to: Roles::Waking.call
      via: direct method call
---

<objective>
Relocate WakeRoleService to Roles::Waking in app/models/roles/waking.rb and update every caller across the codebase. WakeRoleService is the most widely depended-on service -- it is called by jobs, controllers, concerns, and three other services. Moving it first establishes the namespace that all subsequent Phase 30/31 relocations depend on.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Roles::Waking and relocate WakeRoleService logic</name>
  <files>app/models/roles/waking.rb, app/services/wake_role_service.rb</files>
  <reuses>none (this IS the relocation -- the source is app/services/wake_role_service.rb)</reuses>
  <action>
  Create `app/models/roles/waking.rb` containing the exact logic from `app/services/wake_role_service.rb`, wrapped in the `Roles` module namespace:

  ```ruby
  module Roles
    class Waking
      attr_reader :role, :trigger_type, :trigger_source, :context

      def initialize(role:, trigger_type:, trigger_source: nil, context: {})
        @role = role
        @trigger_type = trigger_type.to_s
        @trigger_source = trigger_source
        @context = context.with_indifferent_access
      end

      def call
        # ... exact same implementation as WakeRoleService#call
      end

      def self.call(**args)
        new(**args).call
      end

      private
      # ... exact same private methods
    end
  end
  ```

  The class body is a direct copy of WakeRoleService -- no behavioral changes, no renaming of internal methods. The only change is the class is now `Roles::Waking` inside `module Roles`.

  Then delete `app/services/wake_role_service.rb`.
  </action>
  <verify>Run `ruby -c app/models/roles/waking.rb` to confirm syntax. Run `grep -r "WakeRoleService" app/services/` to confirm the old file is gone.</verify>
  <done>app/models/roles/waking.rb exists with identical logic to the deleted app/services/wake_role_service.rb. The old file no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update all callers and tests to reference Roles::Waking</name>
  <files>
    app/jobs/role_heartbeat_job.rb,
    app/models/concerns/triggerable.rb,
    app/controllers/task_questions_controller.rb,
    app/services/execute_hook_service.rb,
    app/services/goal_evaluation_service.rb,
    app/services/process_validation_result_service.rb,
    test/models/roles/waking_test.rb,
    test/services/execute_hook_service_test.rb
  </files>
  <reuses>none (pure find-and-replace refactoring)</reuses>
  <action>
  Replace every occurrence of `WakeRoleService` with `Roles::Waking` in these files:

  1. **app/jobs/role_heartbeat_job.rb** line 10: `WakeRoleService.call(` -> `Roles::Waking.call(`
  2. **app/models/concerns/triggerable.rb** line 9: `WakeRoleService.call(` -> `Roles::Waking.call(`
  3. **app/controllers/task_questions_controller.rb** line 27: `WakeRoleService.call(` -> `Roles::Waking.call(`
  4. **app/services/execute_hook_service.rb** line 53: `WakeRoleService.call(` -> `Roles::Waking.call(`
  5. **app/services/goal_evaluation_service.rb** line 158: `WakeRoleService.call(` -> `Roles::Waking.call(`
  6. **app/services/process_validation_result_service.rb** line 75: `WakeRoleService.call(` -> `Roles::Waking.call(`

  Then relocate the test file:
  7. **Move** `test/services/wake_agent_service_test.rb` to `test/models/roles/waking_test.rb`
     - Change class name from `WakeRoleServiceTest` to `Roles::WakingTest`
     - Replace every `WakeRoleService.call(` with `Roles::Waking.call(`
     - Keep all test methods, assertions, and setup logic identical

  8. **test/services/execute_hook_service_test.rb** line 58: Update the test name string from "WakeRoleService" to "Roles::Waking" (this is just a test description string, the actual test logic uses HeartbeatEvent counts and does not reference the class name directly -- only the test name string mentions it).

  After all replacements, verify no application code (outside .ariadna_planning/) references `WakeRoleService`:
  `grep -r "WakeRoleService" app/ test/ lib/ config/ --include="*.rb" --include="*.erb"`
  This must return zero results.
  </action>
  <verify>Run `bin/rails test test/models/roles/waking_test.rb test/services/execute_hook_service_test.rb test/jobs/role_heartbeat_job_test.rb` -- all tests must pass. Run `grep -r "WakeRoleService" app/ test/ --include="*.rb"` -- must return zero results.</verify>
  <done>All 6 caller files reference Roles::Waking. Test file relocated to test/models/roles/waking_test.rb with updated class name. Zero references to WakeRoleService remain in application or test code. All tests pass.</done>
</task>
</tasks>

<success_criteria>
- `Roles::Waking.call(role:, trigger_type:)` works identically to the old `WakeRoleService.call`
- All callers (RoleHeartbeatJob, Triggerable, TaskQuestionsController, ExecuteHookService, GoalEvaluationService, ProcessValidationResultService) use `Roles::Waking`
- `bin/rails test test/models/roles/waking_test.rb` passes (all 13 tests)
- `grep -r "WakeRoleService" app/ test/ --include="*.rb"` returns zero results
- `bin/rails test` full suite passes with no regressions
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/29-roles-domain/29-01-SUMMARY.md`
</output>
