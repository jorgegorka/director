---
phase: 12-cleanup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - docs/paperclip-clone.md
  - .ariadna_planning/PROJECT.md
  - .ariadna_planning/MILESTONES.md
  - Gemfile
  - Gemfile.lock
  - app/javascript/controllers/hello_controller.js
  - app/controllers/home_controller.rb
  - app/views/home/show.html.erb
  - test/controllers/home_controller_test.rb
  - test/application_system_test_case.rb
  - app/models/concerns/.keep
  - app/controllers/concerns/.keep
autonomous: true
user_setup: []
domain: general
must_haves:
  truths:
    - "CLAUDE.md Hard Constraints section says SQLite as primary database, not PostgreSQL"
    - "README.md tech stack and prerequisites reference SQLite, no PostgreSQL mentioned"
    - "PROJECT.md Constraints section references SQLite as primary database"
    - "docs/paperclip-clone.md Director description says SQLite, not PostgreSQL"
    - "No hello_controller.js exists in app/javascript/controllers/"
    - "No HomeController, home view, or home_controller_test exist in the codebase"
    - "No capybara or selenium-webdriver gems in Gemfile"
    - "bin/ci passes green with all tests, rubocop, brakeman, and security audits"
  artifacts:
    - path: CLAUDE.md
      provides: "Updated hard constraints reflecting SQLite as primary database"
    - path: README.md
      provides: "Updated tech stack and prerequisites for SQLite-only setup"
    - path: .ariadna_planning/PROJECT.md
      provides: "Updated constraints and milestone description for SQLite"
    - path: docs/paperclip-clone.md
      provides: "Updated Director description to reference SQLite instead of PostgreSQL"
    - path: .ariadna_planning/MILESTONES.md
      provides: "Updated v1.1 milestone to reflect completion status"
    - path: Gemfile
      provides: "Cleaned test group without system test gems"
  key_links:
    - from: CLAUDE.md
      to: database.yml
      via: "Hard Constraints database entry matches actual adapter config"
    - from: README.md
      to: Gemfile
      via: "Prerequisites match actual gem dependencies (sqlite3, no pg)"
---

<objective>
Update all project documentation to reflect the SQLite stack and remove dead code accumulated during v1.0 development. After this plan, a developer evaluating Director sees accurate docs (SQLite everywhere), a clean codebase (no scaffolding leftovers), and a green CI suite. This completes requirements CLN-01, CLN-02, and CLN-03.
</objective>

<execution_context>
@~/.claude/ariadna/workflows/execute-plan.md
@~/.claude/ariadna/templates/summary.md
</execution_context>

<context>
@.ariadna_planning/PROJECT.md
@.ariadna_planning/ROADMAP.md
@.ariadna_planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update project documentation to reflect SQLite stack</name>
  <files>
    CLAUDE.md
    README.md
    docs/paperclip-clone.md
    .ariadna_planning/PROJECT.md
    .ariadna_planning/MILESTONES.md
  </files>
  <reuses>none (documentation-only changes, no code reuse applicable)</reuses>
  <action>
    Update each file to replace stale PostgreSQL references with SQLite. Read each file first, then apply targeted edits.

    **CLAUDE.md** (line 17):
    - Change: `- **Database**: PostgreSQL (primary), SQLite for Solid Queue/Cache/Cable`
    - To: `- **Database**: SQLite for everything (primary + Solid Queue/Cache/Cable)`
    - Also update the Deployment line if it mentions PostgreSQL (it currently says "Kamal + Docker" which is fine)

    **README.md**:
    - Line 29: Change `- PostgreSQL (primary) + SQLite (Solid Queue, Solid Cache, Solid Cable)` to `- SQLite (primary + Solid Queue, Solid Cache, Solid Cable)`
    - Line 36: Change `**Prerequisites:** Ruby 3.4 and PostgreSQL. No Node.js required.` to `**Prerequisites:** Ruby 3.4. No Node.js or external database required.`

    **docs/paperclip-clone.md**:
    - Line 7: Change `PostgreSQL, and Solid Queue` to `SQLite, and Solid Queue`
    - Line 16: Change `rails new director --database=postgresql --css=tailwind` to `rails new director --database=sqlite3 --css=tailwind` (this is the bootstrap command -- update to reflect current reality)
    - Line 22: Change `| \`pg\` | PostgreSQL |` to `| \`sqlite3\` | SQLite |`
    - Line 94: Change `PostgreSQL full-text search via \`tsvector\` or \`pg_trgm\`` to `Full-text search (SQLite FTS5 or LIKE queries)`
    - Line 270: Change `\`docker-compose.yml\` with PostgreSQL` to `\`docker-compose.yml\` with SQLite`

    **PROJECT.md**:
    - Line 43: Change `- [ ] Replace PostgreSQL with SQLite for primary database` to `- [x] Replace PostgreSQL with SQLite for primary database` (requirement is now complete)
    - Line 65: Change `**Tech stack**: Rails 8, SQLite (primary + solid gems)` -- this line already says SQLite, verify and leave if correct. If it still mentions PostgreSQL, fix it.
    - Line 84: The Key Decisions table row for "SQLite over PostgreSQL" -- change Outcome from `-- Pending` to `-- Done`
    - Line 88: Change `**Goal:** Replace PostgreSQL with SQLite for the primary database and clean up v1.0 tech debt.` -- update to past tense: `**Goal:** Replaced PostgreSQL with SQLite for the primary database and cleaned up v1.0 tech debt.`
    - Line 93: Change `- Fix any PostgreSQL-specific SQL, column types, or features (jsonb -> json)` to `- Fixed PostgreSQL-specific column types (jsonb -> json) for SQLite compatibility`

    **MILESTONES.md**:
    - Line 15: Change `**Goal:** Replace PostgreSQL with SQLite for the primary database; clean up v1.0 tech debt.` to `**Goal:** Replaced PostgreSQL with SQLite for the primary database; cleaned up v1.0 tech debt.`
    - Move v1.1 from "Current" to "Completed" section if the structure supports it, or mark it as complete.

    **Important:** Do NOT modify ROADMAP.md or STATE.md -- their PostgreSQL mentions are historical records of the migration work (plan names, decision log entries, phase descriptions) and are accurate as written.
  </action>
  <verify>
    Run: `grep -rn "PostgreSQL\|postgresql" CLAUDE.md README.md docs/paperclip-clone.md .ariadna_planning/PROJECT.md .ariadna_planning/MILESTONES.md`
    Expected: No matches (zero output). All PostgreSQL references in these 5 files are eliminated.

    Also verify the files are syntactically valid: `cat CLAUDE.md | head -20` to spot-check formatting.
  </verify>
  <done>
    - CLAUDE.md line 17 says SQLite, not PostgreSQL
    - README.md tech stack says SQLite, prerequisites say "No external database required"
    - docs/paperclip-clone.md bootstrap command and gem table reference sqlite3
    - PROJECT.md constraints and milestone description reference SQLite; Active requirements show checkbox for SQLite migration
    - MILESTONES.md v1.1 goal updated to past tense
    - Zero grep matches for "PostgreSQL" in these 5 files
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and scaffolding leftovers</name>
  <files>
    app/javascript/controllers/hello_controller.js
    app/controllers/home_controller.rb
    app/views/home/show.html.erb
    test/controllers/home_controller_test.rb
    test/application_system_test_case.rb
    Gemfile
    Gemfile.lock
    app/models/concerns/.keep
    app/controllers/concerns/.keep
  </files>
  <reuses>none (removal task -- no new code created)</reuses>
  <action>
    Remove unused files and clean up the Gemfile. Each removal is justified:

    **1. Delete `app/javascript/controllers/hello_controller.js`**
    - Rails default scaffolding Stimulus controller that outputs "Hello World!"
    - Not referenced in any view (zero `data-controller="hello"` in codebase)
    - Run: `rm app/javascript/controllers/hello_controller.js`

    **2. Delete `app/controllers/home_controller.rb`**
    - HomeController has no route in `config/routes.rb` (root route points to `dashboard#show` since Phase 10)
    - Completely superseded by DashboardController
    - Run: `rm app/controllers/home_controller.rb`

    **3. Delete `app/views/home/` directory**
    - Contains `show.html.erb` which is orphaned (HomeController has no route)
    - Run: `rm -r app/views/home/`

    **4. Delete `test/controllers/home_controller_test.rb`**
    - Tests root route behavior, but all scenarios are already covered by `test/controllers/dashboard_controller_test.rb`:
      - "redirect unauthenticated" -> DashboardController "should require authentication"
      - "show with active company" -> DashboardController "root path shows dashboard"
      - "redirect when no companies" -> DashboardController "should require company"
      - "shows mission" -> DashboardController "should show mission if present"
      - "shows goals link" -> DashboardController tests include goals nav
      - "works without mission" -> unique but tests dead HomeController
    - Run: `rm test/controllers/home_controller_test.rb`

    **5. Delete `test/application_system_test_case.rb`**
    - Project policy: NO system/integration tests (CLAUDE.md)
    - No test/system/ directory exists; no tests inherit from ApplicationSystemTestCase
    - Run: `rm test/application_system_test_case.rb`

    **6. Remove unused gems from Gemfile**
    - Read the Gemfile first
    - In the `group :test` block, remove `gem "capybara"` and `gem "selenium-webdriver"`
    - These support system tests which the project explicitly prohibits
    - Keep the `group :test` block even if empty (Rails convention), or remove it if these are the only two gems
    - Run: `bundle install` to regenerate Gemfile.lock

    **7. Remove unnecessary .keep files**
    - `app/models/concerns/.keep` -- directory contains 6 concern files (auditable.rb, config_versioned.rb, notifiable.rb, tenantable.rb, tree_hierarchy.rb, triggerable.rb)
    - `app/controllers/concerns/.keep` -- directory contains 3 concern files (agent_api_authenticatable.rb, authentication.rb, set_current_company.rb)
    - .keep files only exist to preserve empty directories in git; these directories are populated
    - Run: `rm app/models/concerns/.keep app/controllers/concerns/.keep`
  </action>
  <verify>
    Run these checks:
    1. `test ! -f app/javascript/controllers/hello_controller.js && echo "hello_controller: REMOVED"`
    2. `test ! -f app/controllers/home_controller.rb && echo "HomeController: REMOVED"`
    3. `test ! -d app/views/home && echo "home views: REMOVED"`
    4. `test ! -f test/controllers/home_controller_test.rb && echo "home test: REMOVED"`
    5. `test ! -f test/application_system_test_case.rb && echo "system test case: REMOVED"`
    6. `grep -c "capybara\|selenium" Gemfile` -- expected: 0
    7. `test ! -f app/models/concerns/.keep && echo ".keep models: REMOVED"`
    8. `test ! -f app/controllers/concerns/.keep && echo ".keep controllers: REMOVED"`
    9. `ls app/models/concerns/` -- should list 6 .rb files (no .keep)
    10. `ls app/controllers/concerns/` -- should list 3 .rb files (no .keep)
  </verify>
  <done>
    - hello_controller.js deleted
    - HomeController, its view directory, and its test file deleted
    - application_system_test_case.rb deleted
    - capybara and selenium-webdriver gems removed from Gemfile
    - Gemfile.lock regenerated without capybara/selenium
    - .keep files removed from populated concern directories
    - No new files created; only removals
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full CI suite and verify green</name>
  <files>none (verification only)</files>
  <reuses>bin/ci (existing CI script)</reuses>
  <action>
    Run the full CI suite to verify all changes are clean:

    1. Run `bin/ci` which executes:
       - `bin/setup --skip-server` (database setup)
       - `bin/rubocop` (style check)
       - `bin/bundler-audit` (gem security)
       - `bin/importmap audit` (JS security)
       - `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` (code security)
       - `bin/rails test` (all tests)
       - `bin/rails db:seed:replant` (seed verification)

    2. If rubocop fails: run `bin/rubocop -a` to auto-fix, then re-run `bin/ci`

    3. If tests fail: the removed home_controller_test.rb had 7 tests. Expected new test count: 674 - 7 = 667 tests. All must pass.

    4. If brakeman warns: investigate and fix the specific warning before re-running.

    **Important:** The test count will decrease because we removed home_controller_test.rb (7 tests). This is expected. The metric that matters is 0 failures, 0 errors.
  </action>
  <verify>
    `bin/ci` exits with status 0.
    Test output shows: 667 runs, 0 failures, 0 errors, 0 skips.
    Rubocop shows: no offenses detected.
    Brakeman shows: 0 warnings.
  </verify>
  <done>
    - bin/ci passes green (exit 0)
    - All remaining tests pass with 0 failures
    - Rubocop reports no offenses
    - Brakeman reports no warnings
    - bundler-audit and importmap audit report no vulnerabilities
    - CLN-03 requirement satisfied: all existing tests pass after migration
  </done>
</task>
</tasks>

<success_criteria>
Phase 12 is complete when ALL of the following are true:
1. `grep -rn "PostgreSQL\|postgresql" CLAUDE.md README.md docs/paperclip-clone.md .ariadna_planning/PROJECT.md .ariadna_planning/MILESTONES.md` returns zero matches
2. None of these files exist: hello_controller.js, home_controller.rb, app/views/home/, home_controller_test.rb, application_system_test_case.rb
3. `grep -c "capybara\|selenium" Gemfile` returns 0
4. `bin/ci` exits with status 0 (rubocop + brakeman + bundler-audit + importmap audit + all tests green)
5. Test count is approximately 667 (674 minus 7 removed home_controller tests), all passing
</success_criteria>

<output>
After completion, create `.ariadna_planning/phases/12-cleanup-verification/12-01-SUMMARY.md`
</output>
