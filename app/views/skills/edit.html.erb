<% content_for(:title, "Edit #{@skill.name}") %>

<section class="form-card form-card--wide">
  <h1>Edit Skill</h1>
  <p class="form-card__subtitle">Update details for "<%= @skill.name %>".</p>
  <%= render "form", skill: @skill %>
</section>
