<% content_for(:title, "New Task") %>

<%= turbo_frame_tag "task_form_modal" do %>
  <section class="form-card glass form-card--wide">
    <h1>New Task</h1>
    <%= render "form", task: @task, cancel: tasks_path %>
  </section>
<% end %>
