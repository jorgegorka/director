<% content_for(:title, "New Role") %>

<section class="form-card glass form-card--wide">
  <h1>New Role</h1>
  <p class="form-card__subtitle">Define a position in your project's org chart.</p>

  <%= render "form", role: @role, reparent_child_id: @reparent_child_id %>
</section>
