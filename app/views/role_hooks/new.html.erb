<% content_for(:title, "New Hook - #{@role.title}") %>

<section class="hooks-page">
  <div class="hooks-page__header">
    <h1>New Hook</h1>
    <p class="hooks-page__subtitle"><%= link_to @role.title, @role %> &rsaquo; <%= link_to "Hooks", role_role_hooks_path(@role) %> &rsaquo; New</p>
  </div>

  <%= render "form", role_hook: @role_hook %>
</section>
