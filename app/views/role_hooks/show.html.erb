<% content_for(:title, @role_hook.name.presence || "Hook Detail") %>

<section class="hook-detail">
  <div class="hook-detail__header">
    <div>
      <h1><%= @role_hook.name.presence || "Unnamed Hook" %></h1>
      <p class="hook-detail__breadcrumb">
        <%= link_to @role.title, @role %> &rsaquo;
        <%= link_to "Hooks", role_role_hooks_path(@role) %> &rsaquo;
        <%= @role_hook.name.presence || "Hook" %>
      </p>
    </div>
    <div class="hook-detail__actions">
      <%= link_to "Edit", edit_role_role_hook_path(@role, @role_hook), class: "btn btn--ghost btn--sm" %>
      <%= button_to "Delete", role_role_hook_path(@role, @role_hook), method: :delete, class: "btn btn--ghost btn--sm hook-detail__delete",
            data: { turbo_confirm: "Delete this hook? This cannot be undone." } %>
    </div>
  </div>

  <div class="hook-detail__body">
    <div class="hook-detail__section">
      <h2>Configuration</h2>
      <dl class="hook-detail__config">
        <div class="hook-detail__config-row">
          <dt>Status</dt>
          <dd><%= hook_status_badge(@role_hook) %></dd>
        </div>
        <div class="hook-detail__config-row">
          <dt>Lifecycle Event</dt>
          <dd><%= lifecycle_event_label(@role_hook.lifecycle_event) %></dd>
        </div>
        <div class="hook-detail__config-row">
          <dt>Action Type</dt>
          <dd><%= action_type_label(@role_hook.action_type) %></dd>
        </div>
        <div class="hook-detail__config-row">
          <dt>Position</dt>
          <dd><%= @role_hook.position %></dd>
        </div>
      </dl>
    </div>

    <div class="hook-detail__section">
      <h2>Action Configuration</h2>
      <dl class="hook-detail__config">
        <% if @role_hook.trigger_agent? %>
          <div class="hook-detail__config-row">
            <dt>Target Role</dt>
            <dd>
              <% if @role_hook.target_role %>
                <%= link_to @role_hook.target_role.title, @role_hook.target_role %>
              <% else %>
                <span class="hook-detail__missing">Role not found</span>
              <% end %>
            </dd>
          </div>
          <% if @role_hook.action_config&.dig("prompt") %>
            <div class="hook-detail__config-row">
              <dt>Prompt</dt>
              <dd><%= @role_hook.action_config["prompt"] %></dd>
            </div>
          <% end %>
        <% elsif @role_hook.webhook? %>
          <div class="hook-detail__config-row">
            <dt>URL</dt>
            <dd><code><%= @role_hook.action_config&.dig("url") %></code></dd>
          </div>
          <% if @role_hook.action_config&.dig("headers").present? %>
            <div class="hook-detail__config-row">
              <dt>Headers</dt>
              <dd>
                <% @role_hook.action_config["headers"].each do |key, value| %>
                  <code><%= key %>: <%= key.downcase.include?("auth") ? "••••••••" : value %></code><br>
                <% end %>
              </dd>
            </div>
          <% end %>
        <% end %>
      </dl>
    </div>

    <div class="hook-detail__section">
      <h2>Executions (<%= @executions_count %>)</h2>
      <% if @recent_executions.any? %>
        <table class="table">
          <thead>
            <tr>
              <th>Status</th>
              <th>Task</th>
              <th>Duration</th>
              <th>Time</th>
            </tr>
          </thead>
          <tbody>
            <% @recent_executions.each do |execution| %>
              <tr>
                <td><%= hook_execution_status_badge(execution) %></td>
                <td><%= link_to execution.task.title, execution.task %></td>
                <td><%= execution.duration_seconds ? "#{execution.duration_seconds}s" : "-" %></td>
                <td><%= time_ago_in_words(execution.created_at) %> ago</td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <% if @executions_count > 5 %>
          <p class="hook-detail__more-note"><%= @executions_count - 5 %> more executions not shown.</p>
        <% end %>
      <% else %>
        <p class="hook-detail__empty-note">No executions yet. This hook will fire when a matching task lifecycle event occurs.</p>
      <% end %>
    </div>
  </div>
</section>
